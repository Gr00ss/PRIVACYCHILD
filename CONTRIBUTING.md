# Участие в разработке

Спасибо за интерес к проекту! Мы приветствуем любой вклад в развитие.

## Как внести вклад

### Сообщение об ошибках

При обнаружении ошибки создайте Issue с описанием:
- Версия Windows
- Версия приложения
- Шаги воспроизведения
- Ожидаемое поведение
- Фактическое поведение
- Логи (без токенов!)

### Предложение улучшений

Создайте Issue с пометкой `enhancement` и опишите:
- Какую проблему решает предложение
- Как это должно работать
- Возможные альтернативы

### Pull Requests

1. **Fork репозиторий**
   ```bash
   git clone https://github.com/YOURUSERNAME/PRIVACYCHILD.git
   cd PRIVACYCHILD
   ```

2. **Создайте ветку для изменений**
   ```bash
   git checkout -b feature/my-new-feature
   ```

3. **Внесите изменения**
   - Следуйте стилю кода проекта
   - Добавьте комментарии для сложной логики
   - Обновите документацию при необходимости

4. **Протестируйте изменения**
   ```powershell
   dotnet build
   dotnet publish -c Release
   # Проверьте работу приложения
   ```

5. **Commit и Push**
   ```bash
   git add .
   git commit -m "Add: описание изменений"
   git push origin feature/my-new-feature
   ```

6. **Создайте Pull Request**
   - Опишите что изменено и зачем
   - Укажите связанные Issue (если есть)

## Стиль кода

### C#

- Используйте PascalCase для публичных членов
- Используйте camelCase для приватных полей
- Префикс `_` для приватных полей
- Async методы должны заканчиваться на `Async`
- Используйте `var` когда тип очевиден

Пример:
```csharp
public class MyService
{
    private readonly ILogger<MyService> _logger;
    private int _counter;
    
    public MyService(ILogger<MyService> logger)
    {
        _logger = logger;
    }
    
    public async Task<int> ProcessDataAsync()
    {
        var result = await GetDataAsync();
        return result;
    }
}
```

### PowerShell

- Используйте полные имена командлетов (не алиасы)
- Добавляйте комментарии для сложных секций
- Используйте `$ErrorActionPreference = "Stop"` в скриптах

## Структура проекта

```
PRIVACYCHILD/
├── .github/
│   └── workflows/          # GitHub Actions CI/CD
├── Bot/                    # Telegram интеграция
├── Data/                   # Работа с БД
├── Models/                 # Модели данных
├── Monitoring/             # Мониторинг системы
├── Security/               # Шифрование и защита
├── Services/               # Общие сервисы
└── Program.cs             # Точка входа
```

## Процесс разработки

### Локальная разработка

1. **Настройка окружения**
   ```powershell
   # Создайте локальный конфиг
   Copy-Item appsettings.template.json appsettings.json
   
   # Укажите свои токены в appsettings.json
   ```

2. **Запуск в режиме разработки**
   ```powershell
   dotnet run
   ```

3. **Отладка**
   - Используйте Visual Studio или Rider
   - Точки останова в критических местах
   - Проверяйте логи в `Logs/`

### Тестирование

Перед отправкой PR протестируйте:

1. **Сборка проходит успешно**
   ```powershell
   dotnet build -c Release
   ```

2. **Публикация работает**
   ```powershell
   dotnet publish -c Release -r win-x64 --self-contained
   ```

3. **Установка/удаление работают**
   ```powershell
   .\install.ps1
   # ... проверить работу ...
   .\uninstall.ps1
   ```

4. **Функционал работает корректно**
   - Мониторинг приложений
   - Мониторинг сети
   - Отправка отчетов в Telegram
   - Команды бота отвечают

## GitHub Actions

Проект использует автоматизированные проверки:

### Build Workflow
- Запускается при push в `main`/`develop`
- Проверяет успешность сборки
- Загружает артефакты

### Release Workflow
- Запускается при создании тега `v*.*.*`
- Собирает Release.zip
- Публикует GitHub Release
- Вычисляет SHA256 checksum

### CodeQL Workflow
- Еженедельный анализ безопасности
- Проверка уязвимостей

## Версионирование

Используем [Semantic Versioning](https://semver.org/):
- **MAJOR** - несовместимые изменения API
- **MINOR** - новый функционал (обратно совместимо)
- **PATCH** - исправления ошибок

Пример создания релиза:
```bash
git tag -a v1.2.3 -m "Release version 1.2.3"
git push origin v1.2.3
```

## Безопасность

### Важно!

- **Никогда не коммитьте** токены/ключи
- `appsettings.json` в `.gitignore`
- Используйте `appsettings.template.json` для примеров
- Проверяйте PR на утечки секретов

### Отчет об уязвимостях

Если нашли уязвимость безопасности:
1. **НЕ создавайте публичный Issue**
2. Напишите напрямую в GitHub Security Advisory
3. Опишите проблему и возможное решение

## Лицензия

Внося вклад, вы соглашаетесь что ваш код будет распространяться под [MIT License](LICENSE).

## Вопросы?

Создайте Discussion в разделе Q&A репозитория.

---

Спасибо за вклад в проект! 🚀
